# Focus Blocker Pro - Python Dependencies
# Enhanced proxy-based productivity enforcement system

# External packages required:
mitmproxy>=10.0.0      # HTTPS proxy for traffic filtering
requests>=2.31.0       # HTTP library for validation
psutil>=5.9.0         # Process monitoring
cryptography>=41.0.0   # Enhanced encryption
watchdog>=3.0.0       # File system monitoring
appdirs>=1.4.4        # Application directories

# Core dependencies (built into Python):
# - tkinter (GUI framework)
# - threading (multithreading support)
# - time (time utilities)
# - os (operating system interface)
# - sys (system-specific parameters)
# - signal (signal handling)
# - secrets (cryptographically strong random numbers)
# - hashlib (hash algorithms)
# - json (JSON encoder/decoder)
# - platform (platform identification)
# - datetime (date and time utilities)
# - subprocess (subprocess management)
# - ctypes (C compatible data types)
# - logging (logging facility)
# - pathlib (object-oriented filesystem paths)
# - configparser (configuration file parser) 